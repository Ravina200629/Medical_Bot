name: Sync to Hugging Face Spaces

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  sync-to-hub:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true

      - name: Set up Git LFS
        run: |
          git lfs install
          git lfs track "data/*.pdf"
          git lfs track "*.pdf"

      # FIXED: Changed from @v1 to the actual release version @v0.2.0
      - name: Push to Hugging Face Hub
        uses: huggingface/hub-sync@v0.2.0
        with:
          github_repo_id: Ravina200629/Medibot  
          huggingface_repo_id: Ravina200629/Medibot  
          hf_token: ${{ secrets.HF_TOKEN }}
          repo_type: space
